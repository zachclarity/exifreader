FROM localstack/localstack:3.0.2

# Install Tesseract OCR so local-executor lambdas can use it
RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        tesseract-ocr \
        tesseract-ocr-eng \
        zip && \
    rm -rf /var/lib/apt/lists/*
